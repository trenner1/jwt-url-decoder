{
  "name": "jwt-url-decoder",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "esbuild src/background/background.ts --bundle --outfile=dist/background.js --target=chrome116"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.5",
    "@types/node": "^22.0.0",
    "esbuild": "^0.28.1",
    "typescript": "^5.6.0",
    "vitest": "^4.1.10"
  }
}
